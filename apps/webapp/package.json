{
  "name": "@launchbot/webapp",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram Mini App: Vite + React + Lightweight Charts",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "test": "vitest run --root ../.. --project webapp"
  },
  "dependencies": {
    "@launchbot/shared": "workspace:*",
    "@launchbot/sim-engine": "workspace:*",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  }
}
