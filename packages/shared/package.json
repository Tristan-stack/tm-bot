{
  "name": "@launchbot/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, constants, zod schemas and bot texts (universal entry) + env and logger (server entry)",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server/index.ts"
  },
  "scripts": {
    "dev": "tsc -b --watch --preserveWatchOutput",
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "test": "vitest run --root ../.. --project shared"
  },
  "dependencies": {
    "@grammyjs/types": "5.0.0",
    "bs58": "^6.0.0",
    "dotenv": "^18.0.1",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "zod": "^4.6.5"
  }
}
